# Security Notes

This is a static site with no backend, login, database, or package dependency chain.
That means its attack surface is low.

Still recommended:
- Use HTTPS on your domain.
- Enable domain lock / registrar protection.
- Avoid adding untrusted third-party scripts.
- Keep all contact links mailto-based unless you add a secure form provider.
